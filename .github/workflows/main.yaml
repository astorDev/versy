on:
  push:
      
jobs:
  ouput_version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - id: version
        uses: astorDev/versy/calver@${{ github.ref }}
      - run: echo "Received version is ${{ steps.version.outputs.version }}"